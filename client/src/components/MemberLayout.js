import React, { useState } from 'react';
import { Coffee, Gift, Home, LogOut, Menu, Receipt, Tag, UserRound, Utensils, X } from 'lucide-react';
import { Link, NavLink, useNavigate } from 'react-router-dom';
export default function MemberLayout({ children }) { const navigate = useNavigate(); const [open, setOpen] = useState(false); const loggedIn = !!localStorage.getItem('member_token'); const close = () => setOpen(false); function logout() { localStorage.removeItem('member_token'); localStorage.removeItem('member_user'); navigate('/member/login'); } return <><header className="member-topbar"><Link to="/member/home" className="brand" onClick={close}><span className="brand-mark"><Coffee size={19} /></span><span>Common Ground<small>member lounge</small></span></Link><button className="menu-toggle" onClick={() => setOpen(!open)} aria-label="Toggle member navigation">{open ? <X size={20} /> : <Menu size={20} />}</button><nav className={open ? 'nav-open' : ''}>{loggedIn ? <><NavLink to="/member/home" onClick={close}><Home size={15} /> Home</NavLink><NavLink to="/member/menu" onClick={close}><Utensils size={15} /> Menu</NavLink><NavLink to="/member/rewards" onClick={close}><Gift size={15} /> Rewards</NavLink><NavLink to="/member/visits" onClick={close}><ReceiptIcon /> Visits</NavLink><NavLink to="/member/offers" onClick={close}><Tag size={15} /> Offers</NavLink><NavLink to="/member/about" onClick={close}><UserRound size={15} /> About</NavLink><NavLink to="/member/profile" onClick={close}><UserRound size={15} /> Profile</NavLink><button className="member-logout" onClick={logout}><LogOut size={15} /> Logout</button></> : <><Link to="/member/login" onClick={close}>Member login</Link><Link to="/member/register" className="header-login" onClick={close}>Join the club <span>→</span></Link>}</nav></header><main className="member-main">{children}</main></>; }
function ReceiptIcon() { return <Receipt size={15} />; }