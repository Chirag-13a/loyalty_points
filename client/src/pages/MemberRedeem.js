import React, { useEffect, useState } from 'react';
import api from '../api/axiosInstance';
export default function MemberRedeem() { const [items, setItems] = useState([]); useEffect(() => { api.get('/member/rewards').then(response => setItems(response.data.items)); }, []); return <div className="member-page page-width"><div className="counter-banner"><span className="eyebrow">REDEEM IN STORE</span><h2>Found something good?</h2><p>Visit the counter and show your account to redeem rewards. Our team will take care of the rest.</p></div><div className="member-page-intro compact"><span className="eyebrow">THE REWARD MENU</span><h1>Pick something<br /><em>good.</em></h1><p>Browse your treats, then bring your account to the café counter.</p></div><div className="visual-catalog">{items.map(item => <article className="visual-reward" key={item._id}><img src={item.imageUrl || 'https://images.unsplash.com/photo-1495474472287-4d71bcdd2085?auto=format&fit=crop&w=900&q=80'} alt={item.name} /><div className="visual-reward-body"><span>{item.category}</span><h2>{item.name}</h2><p>{item.description}</p><div><strong>{item.cost} <small>pts</small></strong><span className="counter-only">Redeem at counter</span></div></div></article>)}</div></div>; }
