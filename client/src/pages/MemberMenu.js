import React, { useEffect, useState } from 'react';
import api from '../api/axiosInstance';
const categories = ['All', 'Coffee', 'Tea', 'Pastries', 'Snacks'];
export default function MemberMenu() { const [items, setItems] = useState([]); const [category, setCategory] = useState('All'); useEffect(() => { api.get('/menu').then(response => setItems(response.data.items)); }, []); const visible = category === 'All' ? items : items.filter(item => item.category === category); return <div className="member-page page-width"><div className="member-page-intro"><span className="eyebrow">THE CAFÉ MENU</span><h1>Take your<br /><em>pick.</em></h1><p>Made fresh, served warm, and always worth staying for.</p></div><div className="menu-tabs">{categories.map(item => <button className={category === item ? 'active' : ''} onClick={() => setCategory(item)} key={item}>{item}</button>)}</div><div className="menu-grid">{visible.map(item => <article className="menu-card" key={item._id}><img src={item.imageUrl} alt={item.name} /><div><span>{item.category}</span><h2>{item.name}</h2><p>{item.description}</p><strong>₹{item.price}</strong></div></article>)}</div>{!visible.length && <div className="member-empty">Nothing on this menu yet.</div>}</div>; }
